<!DOCTYPE html>
<html>
  <head>
    <title><%= document_title(@title) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'admin', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
    <div id="wrapper">
      <%= render 'admin/shared/header' %>
      <div id="container">
        <%= yield %>
      </div>
      <%= render 'shared/footer' %>
    </div>
  </body>
</html>